input {
    //println "input 层"
    StdInput2 {
        codec 'SimpleCodec'
        delimiter "\n"
    }
    SplitFilter {
        field "message"
        if (it['message']=='h,i')
            terminator ','
        else
            terminator '@'
    }
}

filter {

//    println "filter 层"
    SplitFilter {
        field "message"
        if (it['message']=='h,i')
            terminator ','
        else
            terminator '@'
    }
}


output {
//    println "output 层"
    if (it['message']=='hi')
        println "hi output"
    StdOutput {
        codec 'JsonCodec'
    }
    StdOutput {
        if (it['message']=='hi')
            println "hi pi"
        codec 'JsonCodec'
    }
}
